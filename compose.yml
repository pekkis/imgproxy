services:
  thumbor:
    image: ghcr.io/imgproxy/imgproxy:v3.30.0
    restart: always
    networks:
      - thumbor

    ports:
      - 5007:8080

    environment:
      - IMGPROXY_KEY=${IMGPROXY_KEY}
      - IMGPROXY_SALT=${IMGPROXY_SALT}

networks:
  thumbor:
    driver: bridge
